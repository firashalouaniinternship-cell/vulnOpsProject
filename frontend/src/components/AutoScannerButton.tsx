/**
 * Composant React pour l'auto-sélection des scanners
 * Affiche un bouton et recommande les scanners automatiquement
 */

import React, { useState } from 'react';
import useAutoScannerSelection from '../hooks/useAutoScannerSelection';

interface AutoScannerButtonProps {
  repoFullName: string;
  cloneUrl: string;
  repoName: string;
  repoOwner: string;
  onAutoSelect?: (scanners: string[]) => void;
  onAutoScan?: (results: any) => void;
  variant?: 'recommend' | 'scan';
  className?: string;
}

export function AutoScannerButton({
  repoFullName,
  cloneUrl,
  repoName,
  repoOwner,
  onAutoSelect,
  onAutoScan,
  variant = 'recommend',
  className = '',
}: AutoScannerButtonProps) {
  const { selectScanners, autoScan, loading, error, progress } = useAutoScannerSelection();
  const [selectedData, setSelectedData] = useState<any>(null);
  const [showDetails, setShowDetails] = useState(false);
  
  const handleAutoSelect = async () => {
    try {
      const result = await selectScanners(repoFullName, cloneUrl, repoName, repoOwner);
      setSelectedData(result);
      setShowDetails(true);
      onAutoSelect?.(result.suggested_scanners);
    } catch (err) {
      console.error('Auto-select failed:', err);
    }
  };
  
  const handleAutoScan = async () => {
    try {
      const result = await autoScan(repoFullName, cloneUrl, repoName, repoOwner);
      setSelectedData(result);
      setShowDetails(true);
      onAutoScan?.(result);
    } catch (err) {\n      console.error('Auto-scan failed:', err);
    }
  };
  
  const buttonText = variant === 'scan' ? '🤖 Auto Scan' : '🣴 Recommend Scanners';
  const buttonAction = variant === 'scan' ? handleAutoScan : handleAutoSelect;
  
  return (\n    <div className={`auto-scanner-container ${className}`}>\n      <button\n        onClick={buttonAction}\n        disabled={loading}\n        className=\"auto-scanner-button\"\n        title=\"Automatically detect languages and recommend appropriate scanners\"\n      >\n        {loading ? (\n          <>\n            <span className=\"spinner\"></span>\n            {progress || 'Processing...'}\n          </>\n        ) : (\n          buttonText\n        )}\n      </button>\n      \n      {error && (\n        <div className=\"error-message\">\n          ❌ Error: {error}\n        </div>\n      )}\n      \n      {selectedData && showDetails && (\n        <div className=\"scanner-recommendations\">\n          <h3>🔍 Project Analysis</h3>\n          \n          <div className=\"analysis-section\">\n            <h4>Detected Languages</h4>\n            <div className=\"languages-list\">\n              {selectedData.analysis?.languages?.map((lang: string) => (\n                <span key={lang} className=\"badge language\">\n                  {lang}\n                  {selectedData.analysis?.file_counts?.[lang] && (\n                    <span className=\"count\"> ({selectedData.analysis.file_counts[lang]} files)</span>\n                  )}\n                </span>\n              ))}\n            </div>\n          </div>\n          \n          {selectedData.analysis?.frameworks && Object.keys(selectedData.analysis.frameworks).length > 0 && (\n            <div className=\"analysis-section\">\n              <h4>Detected Frameworks</h4>\n              <div className=\"frameworks-list\">\n                {Object.entries(selectedData.analysis.frameworks).map(([lang, frameworks]: [string, any]) => (\n                  <div key={lang} className=\"framework-item\">\n                    <span className=\"framework-lang\">{lang}:</span>\n                    {Array.isArray(frameworks) && frameworks.map((fw: string) => (\n                      <span key={fw} className=\"badge framework\">{fw}</span>\n                    ))}\n                  </div>\n                ))}\n              </div>\n            </div>\n          )}\n          \n          <div className=\"analysis-section\">\n            <h4>Recommended Scanners</h4>\n            <div className=\"scanners-list\">\n              {selectedData.suggested_scanners?.map((scanner: string) => (\n                <span key={scanner} className=\"badge scanner\">\n                  ✓ {scanner.toUpperCase()}\n                </span>\n              ))}\n            </div>\n          </div>\n          \n          <div className=\"confidence-section\">\n            <div className=\"confidence-bar\">\n              <div\n                className=\"confidence-fill\"\n                style={{ width: `${(selectedData.confidence || 0) * 100}%` }}\n              ></div>\n            </div>\n            <p className=\"confidence-text\">\n              Confidence: {((selectedData.confidence || 0) * 100).toFixed(0)}%\n            </p>\n          </div>\n          \n          <div className=\"reasoning-section\">\n            <h4>Reasoning</h4>\n            <p className=\"reasoning-text\">{selectedData.reasoning}</p>\n          </div>\n          \n          {selectedData.source && (\n            <p className=\"source-text\">\n              Source: {selectedData.source === 'openrouter' ? '🤖 OpenRouter LLM' : '⚙️ Default Rules'}\n            </p>\n          )}\n          \n          {variant === 'recommend' ? (\n            <button\n              onClick={() => {\n                handleAutoScan();\n              }}\n              className=\"action-button scan-button\"\n              disabled={loading}\n            >\n              🚀 Launch Selected Scans\n            </button>\n          ) : (\n            <div className=\"scan-results\">\n              {selectedData.scan_results?.map((result: any, idx: number) => (\n                <div key={idx} className={`scan-result ${result.status.toLowerCase()}`}>\n                  <h5>{result.scanner.toUpperCase()}</h5>\n                  <p className=\"status\">Status: {result.status}</p>\n                  {result.status === 'COMPLETED' && result.metrics && (\n                    <div className=\"metrics\">\n                      <span className=\"metric critical\">High: {result.metrics.high_count}</span>\n                      <span className=\"metric warning\">Medium: {result.metrics.medium_count}</span>\n                      <span className=\"metric info\">Low: {result.metrics.low_count}</span>\n                    </div>\n                  )}\n                  {result.error && <p className=\"error\">{result.error}</p>}\n                </div>\n              ))}\n            </div>\n          )}\n          \n          <button\n            onClick={() => setShowDetails(false)}\n            className=\"close-button\"\n            title=\"Close details\"\n          >\n            ✕\n          </button>\n        </div>\n      )}\n      \n      <style>{`\n        .auto-scanner-container {\n          position: relative;\n          display: inline-block;\n        }\n        \n        .auto-scanner-button {\n          display: flex;\n          align-items: center;\n          gap: 8px;\n          padding: 10px 16px;\n          background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);\n          color: white;\n          border: none;\n          border-radius: 6px;\n          font-size: 14px;\n          font-weight: 600;\n          cursor: pointer;\n          transition: all 0.3s ease;\n          box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);\n        }\n        \n        .auto-scanner-button:hover:not(:disabled) {\n          transform: translateY(-2px);\n          box-shadow: 0 6px 20px rgba(102, 126, 234, 0.6);\n        }\n        \n        .auto-scanner-button:disabled {\n          opacity: 0.7;\n          cursor: not-allowed;\n        }\n        \n        .spinner {\n          display: inline-block;\n          width: 16px;\n          height: 16px;\n          border: 2px solid rgba(255, 255, 255, 0.3);\n          border-top-color: white;\n          border-radius: 50%;\n          animation: spin 0.8s linear infinite;\n        }\n        \n        @keyframes spin {\n          to { transform: rotate(360deg); }\n        }\n        \n        .error-message {\n          margin-top: 8px;\n          padding: 10px;\n          background-color: #fee;\n          color: #c33;\n          border-radius: 4px;\n          font-size: 13px;\n        }\n        \n        .scanner-recommendations {\n          position: absolute;\n          top: 100%;\n          left: 0;\n          margin-top: 8px;\n          background: white;\n          border: 1px solid #ddd;\n          border-radius: 8px;\n          padding: 20px;\n          box-shadow: 0 8px 32px rgba(0, 0, 0, 0.15);\n          min-width: 400px;\n          max-width: 600px;\n          z-index: 1000;\n          animation: slideUp 0.3s ease;\n        }\n        \n        @keyframes slideUp {\n          from {\n            opacity: 0;\n            transform: translateY(10px);\n          }\n          to {\n            opacity: 1;\n            transform: translateY(0);\n          }\n        }\n        \n        .scanner-recommendations h3 {\n          margin: 0 0 16px 0;\n          font-size: 18px;\n          color: #333;\n        }\n        \n        .scanner-recommendations h4 {\n          margin: 12px 0 8px 0;\n          font-size: 14px;\n          color: #666;\n          text-transform: uppercase;\n          letter-spacing: 0.5px;\n        }\n        \n        .analysis-section {\n          margin-bottom: 16px;\n        }\n        \n        .badge {\n          display: inline-block;\n          margin: 4px 4px 4px 0;\n          padding: 6px 12px;\n          border-radius: 20px;\n          font-size: 12px;\n          font-weight: 600;\n        }\n        \n        .badge.language {\n          background-color: #e3f2fd;\n          color: #1976d2;\n        }\n        \n        .badge.framework {\n          background-color: #f3e5f5;\n          color: #7b1fa2;\n        }\n        \n        .badge.scanner {\n          background-color: #e8f5e9;\n          color: #388e3c;\n        }\n        \n        .badge .count {\n          font-size: 11px;\n          opacity: 0.7;\n        }\n        \n        .framework-item {\n          margin: 8px 0;\n          display: flex;\n          align-items: center;\n          gap: 8px;\n        }\n        \n        .framework-lang {\n          font-weight: 600;\n          color: #555;\n          min-width: 80px;\n        }\n        \n        .confidence-section {\n          margin: 16px 0;\n        }\n        \n        .confidence-bar {\n          width: 100%;\n          height: 8px;\n          background-color: #eee;\n          border-radius: 4px;\n          overflow: hidden;\n        }\n        \n        .confidence-fill {\n          height: 100%;\n          background: linear-gradient(90deg, #4caf50, #8bc34a);\n          transition: width 0.3s ease;\n        }\n        \n        .confidence-text {\n          margin: 8px 0 0 0;\n          font-size: 12px;\n          color: #666;\n        }\n        \n        .reasoning-section {\n          margin-top: 12px;\n          padding: 12px;\n          background-color: #f5f5f5;\n          border-radius: 4px;\n        }\n        \n        .reasoning-text {\n          margin: 0;\n          font-size: 13px;\n          color: #555;\n          line-height: 1.5;\n        }\n        \n        .source-text {\n          margin-top: 8px;\n          font-size: 11px;\n          color: #999;\n          text-align: right;\n        }\n        \n        .action-button {\n          width: 100%;\n          margin-top: 12px;\n          padding: 10px;\n          background-color: #4caf50;\n          color: white;\n          border: none;\n          border-radius: 4px;\n          font-size: 14px;\n          font-weight: 600;\n          cursor: pointer;\n          transition: background-color 0.3s ease;\n        }\n        \n        .action-button:hover {\n          background-color: #45a049;\n        }\n        \n        .action-button:disabled {\n          opacity: 0.6;\n          cursor: not-allowed;\n        }\n        \n        .scan-results {\n          margin-top: 12px;\n          display: flex;\n          flex-direction: column;\n          gap: 8px;\n        }\n        \n        .scan-result {\n          padding: 12px;\n          border-left: 4px solid #ddd;\n          border-radius: 4px;\n          background-color: #fafafa;\n        }\n        \n        .scan-result.completed {\n          border-left-color: #4caf50;\n          background-color: #f1f8f5;\n        }\n        \n        .scan-result.failed {\n          border-left-color: #f44336;\n          background-color: #fef5f5;\n        }\n        \n        .scan-result.running {\n          border-left-color: #ff9800;\n          background-color: #fff8f0;\n        }\n        \n        .scan-result h5 {\n          margin: 0 0 6px 0;\n          font-size: 13px;\n          color: #333;\n        }\n        \n        .scan-result .status {\n          margin: 0 0 6px 0;\n          font-size: 12px;\n          color: #666;\n        }\n        \n        .scan-result .metrics {\n          display: flex;\n          gap: 12px;\n          font-size: 12px;\n        }\n        \n        .metric {\n          padding: 4px 8px;\n          border-radius: 3px;\n        }\n        \n        .metric.critical {\n          background-color: #ffebee;\n          color: #c62828;\n        }\n        \n        .metric.warning {\n          background-color: #fff3e0;\n          color: #e65100;\n        }\n        \n        .metric.info {\n          background-color: #e3f2fd;\n          color: #01579b;\n        }\n        \n        .scan-result .error {\n          margin-top: 6px;\n          font-size: 12px;\n          color: #d32f2f;\n        }\n        \n        .close-button {\n          position: absolute;\n          top: 8px;\n          right: 8px;\n          width: 24px;\n          height: 24px;\n          padding: 0;\n          background: none;\n          border: none;\n          font-size: 18px;\n          color: #999;\n          cursor: pointer;\n          transition: color 0.2s ease;\n        }\n        \n        .close-button:hover {\n          color: #333;\n        }\n      `}</style>\n    </div>\n  );\n}\n\nexport default AutoScannerButton;
